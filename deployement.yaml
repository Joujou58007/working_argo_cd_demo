# apiVersion: rbac.authorization.k8s.io/v1
# kind: ClusterRoleBinding
# metadata:
#   name: flannel-node-list-fix
# roleRef:
#   apiGroup: rbac.authorization.k8s.io
#   kind: ClusterRole
#   name: system:node
# subjects:
# - kind: Group
#   name: system:nodes
#   apiGroup: rbac.authorization.k8s.io
# ---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: landing-moon
  namespace: default
spec:
  selector:
    matchLabels:
      app: landing-moon
  template:
    metadata:
      labels:
        app: landing-moon
    spec:
      containers:
      - name: landing-moon
        image: busybox
        command: ["sh", "-c", "echo 'One small step for man, but a giant leap of the project!' && sleep infinity"]
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: apollo
  namespace: default
spec:
  selector:
    matchLabels:
      app: apollo
  template:
    metadata:
      labels:
        app: apollo
    spec:
      nodeSelector:
        kubernetes.io/hostname: mx6qmpic
      containers:
      - name: apollo
        image: docker.io/loulou3333444/apollo@sha256:9275c0f797a9b75a9bda8a5e5210a17770968d0ee9830ccad716e978a663de7f
        imagePullPolicy: Always
      #   securityContext:
      #     privileged: true
      #   volumeMounts:
      #   - mountPath: /dev/spidev0.0
      #     name: spidev
      # volumes:
      # - name: spidev
      #   hostPath:
      #     path: /dev/spidev0.0
      #     type: CharDevice