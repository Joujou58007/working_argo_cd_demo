# apiVersion: rbac.authorization.k8s.io/v1
# kind: ClusterRoleBinding
# metadata:
#   name: flannel-node-list-fix
# roleRef:
#   apiGroup: rbac.authorization.k8s.io
#   kind: ClusterRole
#   name: system:node
# subjects:
# - kind: Group
#   name: system:nodes
#   apiGroup: rbac.authorization.k8s.io
# ---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: landing-moon
  namespace: default
spec:
  selector:
    matchLabels:
      app: landing-moon
  template:
    metadata:
      labels:
        app: landing-moon
    spec:
      containers:
      - name: landing-moon
        image: busybox
        command: ["sh", "-c", "echo 'One small step for man, but a giant leap of the project!' && sleep infinity"]
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: apollo
  namespace: default
spec:
  selector:
    matchLabels:
      app: apollo
  template:
    metadata:
      labels:
        app: apollo
    spec:
      nodeSelector:
        kubernetes.io/hostname: mx6qmpic
      containers:
      - name: apollo
        image: docker.io/loulou3333444/apollo@sha256:12edcd345df8e0e038245f703404e171a22686a5e9f992dc98c7846a736a5eb6
        imagePullPolicy: Always
      #   securityContext:
      #     privileged: true
      #   volumeMounts:
      #   - mountPath: /dev/spidev0.0
      #     name: spidev
      # volumes:
      # - name: spidev
      #   hostPath:
      #     path: /dev/spidev0.0
      #     type: CharDevice